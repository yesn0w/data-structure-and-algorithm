package class18;

public class TakingCards {

    // 暴力递归
    // 返回获胜者的最大分数
    public static int maxPoints(int[] arr) {
        if (arr == null || arr.length == 0) {
            return -1;
        }
        int
    }
    // 自顶向下的动态规划（傻缓存）
    // 严格动态规划

}
