package class26;

public class C03_ZeroLeftOneStringNumber {

    /**
     * 由0和1组成字符串，其中
     * 任何0的左边都有1
     * @param n 长度
     * @return 达标的个数
     */
    public static int getNum(int n) {
        // f(n) = f(n-1) + f(n-2)
        return 0;
    }
}
